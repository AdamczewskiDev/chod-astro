{
  "name": "chod-astro",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "dev:cf": "npm run build && wrangler pages dev dist --port 8788 --ip 127.0.0.1"
  },
  "dependencies": {
    "@fontsource/courier-prime": "^5.3.0",
    "astro": "^7.0.3"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
